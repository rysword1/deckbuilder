import React from "react";
import Deck from "./Deck";

function Decks({ decks }) {
    
    return (
        <div>
            <Deck decks={decks} />
            hi
        </div>
    );
}

export default Decks;